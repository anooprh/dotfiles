; ~/.emacs.d/my-noexternals.el

;; Setting Brackground color Yellow, foreground black
(set-foreground-color "yellow")
(set-background-color "black")

;disable backup
(setq backup-inhibited t)
;disable auto save
(setq auto-save-default nil)